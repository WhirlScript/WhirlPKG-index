# @format

name: "update"
on:
    push:
        branches:
            - "test"
    #schedule:
    #  - cron: 0 0,4,8,12,16,20 * * *
jobs:
    runscript:
        runs-on: ubuntu-latest
        steps:
            - name: "checkout"
              uses: actions/checkout@v4

            - name: "yarn add"
              run: pwd && ls -a && yarn

            - name: "update"
              run: npx ts-node ./scripts/index.js

            - name: "show ssh key"
              run: echo {{ secret.SSH_KEY }}
